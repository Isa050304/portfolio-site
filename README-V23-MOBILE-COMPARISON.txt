V23 MOBILE COMPARISON FIX

- Mobile Before/After drag is bound directly to the visible divider.
- Pointer capture keeps the divider moving while the finger leaves the handle area.
- The rest of the comparison keeps normal vertical page scrolling.
- Old touch fallback only runs on browsers without Pointer Events, preventing duplicate Safari gesture handlers.
- CSS/JS cache version bumped to v23 on project pages.
